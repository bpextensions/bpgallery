{
    "scripts": {
        "build": "phing",
        "clean": "phing clean",
        "test:server:start": "docker-compose up -d",
        "test:server:cleanup": "docker-compose down -v"
    },
    "require-dev": {
        "phing/phing": "2.*"
    },
    "require": {
        "ext-json": "*",
        "php": "^7.2"
    }
}